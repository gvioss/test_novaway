{% extends 'base.html.twig' %}

{% block content %}

        <h2>Book: List</h2>

        <a href="/book/add">Ajouter un livre</a>

        <table class="table">
            <thead>
            <tr>
                <th>ISNB</th>
                <th>Titre</th>
                <th>Auteur</th>
                <th>Date de parution</th>
                <th>Nombre de page</th>
                <th>Prix</th>
                <th></th>
            </tr>
            </thead>
            <tbody>
            {% for book in books %}
                <tr>
                    <th>{{ book.isnb }}</th>
                    <td>{{ book.title }}</td>
                    <td>{{ book.author.name }}</td>
                    <td>{{ book.releasedate|date('j F Y') }}</td>
                    <td>{{ book.pagenumber }}</td>
                    <td>{{ book.price }}</td>
                    <td>
                        <a href="/book/detail/{{ book.id }}">Détail</a>
                        <a href="/book/edit/{{ book.id }}">Modifier</a>
                        <a href="/book/delete/{{ book.id }}">Supprimer</a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

{% endblock %}

